# Prerequisites

Python 3, stdlib, plus `pypdf` (`pip install pypdf`) -- needed only for the
live "what it means / remediation" fetch (see [owasp-reference.md](owasp-reference.md)),
not for attack-sending itself.
