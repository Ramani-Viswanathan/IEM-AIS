# governance

Reserved for Phase 6 of `Project DOCS/IEM-AIS-Platform-Evolution-Plan.md`
-- not built yet.

Will hold `controls.json` (or reuse each skill's existing `remediation`
text as the control description) plus a manually-updated status field
per (target, risk_id) -- the evidentiary spine (control -> evidence ->
finding -> remediation status -> retest), not a workflow/ticketing
engine.

See the plan doc for the full design before adding anything here.
